const USER_ROLE_ENUM = {
    ADMIN: 'admin',
    CUSTOMER: 'customer',
    FLIGHT_COMPANY: 'flight_company'
};

const NOTIF_STATUS_ENUM = {
    PENDING: "pending",
    CANCELLED: "cancelled",
    BOOKED: "booked"
};

module.exports = {
    NOTIF_STATUS_ENUM,
    USER_ROLE_ENUM
};